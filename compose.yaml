services:
  app:
    image: swipl:stable
    working_dir: /app/backend
    volumes:
      - .:/app:ro
    command: ["swipl", "-g", "start(8080), thread_get_message(_)", "server.pl"]
    ports:
      - "8000:8080"
